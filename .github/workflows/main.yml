name: Deploy

on:
  repository_dispatch:
    types: [retry-deploy]

  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: print
        run: echo "Deploying terraform"

      - name: print
        run: echo "Deploying changes"

      - name: Deploy
        run: |
          echo "Deploying..."
          # Simulate failure
          exit 1
          
