name: Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: print
        run: echo "HAHAHAHA"

      - name: Deploy
        run: |
          echo "Deploying..."
          # Simulate failure
          exit 1
